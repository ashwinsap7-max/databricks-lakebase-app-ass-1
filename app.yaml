command:
  - streamlit
  - run
  - app.py
  - --server.port=8080

resources:
  - name: lakebase-postgres
    database:
      project: data-experts-postgres
      branch: summer-tooth
      database: support-system